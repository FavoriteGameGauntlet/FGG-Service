type: object
properties:
  state:
    type: string
    enum: [ "created", "running", "paused", "finished" ]
  remainingTime:
    $ref: "./Duration.yaml"
  duration:
    $ref: "./Duration.yaml"
  lastActionDate:
    type: string
    format: date-time
required: [ state, remainingTime, duration, lastActionDate ]