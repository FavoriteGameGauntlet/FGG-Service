allOf:
  - $ref: "./WheelEffect.yaml"
  - type: object
    properties:
      position:
        type: integer
        format: int
      rollDate:
        type: string
        format: date-time
    required: [ rollDate ]