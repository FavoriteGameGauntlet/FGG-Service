allOf:
  - $ref: "./PointChange.yaml"
  - type: object
    properties:
      actualChangeValue:
        type: integer
        format: int
      finalValue:
        $ref: "./Points.yaml"
      changeDate:
        type: string
        format: date-time
    required: [ actualChangeValue, finalValue, changeDate ]